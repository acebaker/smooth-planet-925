	<div class="work">
		<% project.people.each do |person| %>
			<% @pageuri = person.id %>
		<% end %>
		
		<%= link_to project.name, person_path(@pageuri, :anchor => project.name.downcase.gsub(/ /,'_')) %>
		
	</div>

